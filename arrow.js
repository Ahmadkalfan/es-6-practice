// const doubleIt=(x)=>x*2
// const result=doubleIt(2);
// console.log(result);
// this is the way to write arrow funciton
const doMath=(x,y)=>{
    const sum=x+y;
    const diff=x-y;
    const result=sum*diff;
    return result
}
const result= doMath(5,3);
console.log(result);
